# Local Java runtime

Temurin JDK 21 is downloaded locally here so Neo4j can run without changing the system Java installation.

Downloaded package:

```text
temurin-jdk-21-windows-x64.zip
```

The JDK zip and extracted runtime are intentionally ignored by Git because they are large binaries.
